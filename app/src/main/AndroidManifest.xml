<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mb.ko">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.CompetitorNumberActivity" />
        <activity android:name=".Activities.WorkActivity" />
        <activity android:name=".Activities.SummaryResultActivity" />
        <activity android:name=".Activities.StageNumberActivity" />
        <activity android:name=".Activities.TimerActivity" />
        <activity android:name=".Activities.TimerSettingsActivity" />
        <activity android:name=".Fragments.PassActivity"></activity>
    </application>

</manifest>